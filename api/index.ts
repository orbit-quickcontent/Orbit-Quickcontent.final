import app from '../backend/src/server';

// Vercel serverless function handler
// Express app is compatible with Vercel's Node.js runtime
// The app handles all routing and middleware setup

export default app;
